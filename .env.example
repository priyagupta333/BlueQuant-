# SECURITY
SECRET_KEY='your-django-secret-key-here'
DEBUG=True

# Database (Neon PostgreSQL or Render PostgreSQL)
DATABASE_URL=postgresql://user:password@host:port/dbname?sslmode=require

# Allowed hosts (comma-separated, include your Render domain)
ALLOWED_HOSTS=127.0.0.1,localhost,.onrender.com

# CSRF Trusted Origins (for Render deployment)
CSRF_TRUSTED_ORIGINS=https://your-app-name.onrender.com

# Email Configuration
EMAIL_BACKEND='django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST='smtp.gmail.com'
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER='your-email@gmail.com'
EMAIL_HOST_PASSWORD='your-app-password'
DEFAULT_FROM_EMAIL='your-email@gmail.com'

# Blockchain Private Key (for production use)
PRIVATE_KEY=your-blockchain-private-key

# Hardhat default test account private key (for local development only)
HARDHAT_PRIVATE_KEY= key
